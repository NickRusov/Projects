@model FLS.LocalWiki.Models.NewComment

        @if (Model != null)
        {
            @*<table> @foreach (var comment in Model.Comments)
                    {
                        <tr>
                            <td>
                                <p>@comment</p>
                            </td>
                        </tr>
                    }
            </table>*@
        }
        @Model?.Comment
        @Model?.ArticleId
       @* <script src="@Url.Content("~/App_Code/jquery.unobtrusive-ajax.min.js")" type="text/javascript"></script>*@
        @*@using (Html.BeginForm("AddComment", "Article", FormMethod.Post, Model))
        {
            <fieldset>
                <legend>Form to add a comment</legend>
                <ol>
                    <li>
                        @Html.LabelFor(m => m.Comment)
                        @Html.TextBoxFor(m => m.Comment)
                        @Html.HiddenFor(m => m.ArticleId)
                    </li>
                </ol>
                <input type="submit" value="Add comment" />
            </fieldset>
        }*@
